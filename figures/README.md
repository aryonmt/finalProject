# Figures

PNGs here are produced by `python scripts/make_figures.py`. Catalogue and styling: [docs/figures.md](../docs/figures.md).

`fig_tsne_*.png` files are optional. Rebuilding them needs local `embeddings_*.npz` (not in git) and `python scripts/plot_tsne.py`.
